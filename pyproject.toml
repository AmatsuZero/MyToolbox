[project]
name = "mytoolbox"
version = "0.1.0"
description = "基于OpenAI Whisper的语音转文字工具"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai-whisper",
    "faster-whisper>=1.0.0",
    "psutil",
    "torch",
    "torchaudio",
    "PyYAML",
]